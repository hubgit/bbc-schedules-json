{
  "name": "bbc-programmes-json",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "express": "^4.16.2",
    "fetch-resource": "^0.3.2"
  },
  "devDependencies": {
    "jest": "^22.0.4",
    "nodemon": "^1.14.6"
  },
  "scripts": {
    "deploy": "now --public && now alias",
    "start": "node src/app.js",
    "start:watch": "nodemon src/app.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "files": [
    "src"
  ],
  "now": {
    "alias": "bbc-programmes-json.now.sh"
  }
}
